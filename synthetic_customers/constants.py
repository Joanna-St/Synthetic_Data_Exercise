# Shared constants used across the pipeline.
# Default values here are overridden by CLI arguments.

LOGFILE = "run.log"
LOGDATEFMT = "%d-%m-%Y %H:%M:%S"
DEFAULT_COUNT = 30
DEFAULT_OUTPUT = "my_output"
